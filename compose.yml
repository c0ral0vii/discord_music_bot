services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: darkmusic-bot
    restart: unless-stopped
    env_file:
      - .env
    working_dir: /app
    command: ["sh", "./scripts/entrypoint-bot.sh"]
